/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "www.ifms.edu.br",
            "fakestoreapi.com"
        ]
    }
};

export default nextConfig;
